name: Playwright CI Report

on:
  schedule:
    - cron: '0 6 * * *' # Runs daily at 06:00 UTC
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Run Playwright tests
        run: npx playwright test --reporter=html

      - name: Upload Playwright HTML report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-html-report
          path: playwright-report
          retention-days: 7

      - name: Create email body
        if: always()
        run: |
          REPORT_URL="https://github.com/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
          cat <<EOT > email_body.html
          <html>
          <body style="font-family:Arial, sans-serif; color:#333;">
            <h2>Daily Playwright Test Report</h2>
            <p><strong>Job Status:</strong> <span style="color:${{ job.status == 'success' && 'green' || 'red' }};">${{ job.status }}</span></p>
            <p>Run ID: <strong>${GITHUB_RUN_ID}</strong></p>
            <p><a href="${REPORT_URL}" target="_blank" style="color:#1a73e8; text-decoration:none; font-weight:bold;">Click here to view &amp; download the HTML report</a></p>
          </body>
          </html>
          EOT

      - name: Send Email with report link
        if: always()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: ${{ secrets.SMTP_SERVER }}
          server_port: ${{ secrets.SMTP_PORT }}
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          subject: "Playwright Test Report - Run #${{ github.run_id }} - ${{ github.event.head_commit.timestamp }} (Status: ${{ job.status }})"
          to: ${{ secrets.EMAIL_TO }}
          from: ${{ secrets.EMAIL_USERNAME }}
          body: file://email_body.html
